{
   "name": "root",
   "version": "1.0.0",
   "description": "",
   "main": "index.js",
   "scripts": {
      "dev": "cd frontend && npm start",
      "build": "cd frontend && npm run build",
      "install-client": "cd frontend && npm install",
      "heroku-postbuild": "npm run install-client && npm run build",
      "start": "serve -s build -l 8080"
   },
   "author": "Agile Monsters",
   "license": "ISC",
   "dependencies": {
      "concurrently": "^5.1.0",
      "serve": "^11.3.0"
   }
}
